export type Product = {
    id: number;
    name: string;
    isFeatured: boolean;
    category: string;
};
  
export type User = {
    id: number;
    name: string;
    email: string;
    password: string;
};